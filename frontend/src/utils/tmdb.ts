/**
 * TMDB được gọi qua backend để API key không bị gửi xuống trình duyệt.
 * File được giữ lại làm điểm mở rộng chung cho các helper TMDB không chứa bí mật.
 */
export const clearTmdbApiKeyCache = () => undefined;
